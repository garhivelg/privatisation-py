extends ../_priv.pug

block content
  .row
    .col-md-6
      include _register_form.pug
    .col-md-6
      include _children.pug
      +children('Дела:', items, "{{ url_for('edit_case', case_id=i.id) }}", "{{ url_for('edit_case', register_id=register.id) }}", "В этой описи нет дел")
