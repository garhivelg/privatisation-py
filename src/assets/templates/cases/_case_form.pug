form(method="post", action="{{ url_for('edit_case', case_id=case.id) }}")
  | {{ form.hidden_tag() }}
  p.sized-field {{ form.register.label }} {{ form.register(class="form-control") }}
  p.sized-field {{ form.book_id.label }} {{ form.book_num(class="form-control", size=7) }}
  p.sized-field {{ form.facility.label }} {{ form.facility(class="form-control") }}
  p.sized-field
    | {{ form.description.label }}
    | {{ form.description(rows=5, cols=40, class="form-control") }}
  p
    input(type="submit", class="btn btn-success", value="Сохранить")
